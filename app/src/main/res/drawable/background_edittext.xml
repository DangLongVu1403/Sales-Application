<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Màu nền -->
    <stroke
        android:width="2dp"
        android:color="@color/grey"/> <!-- Màu viền -->
    <corners android:radius="6dp"/> <!-- Độ bo góc -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp"/> <!-- Khoảng cách nội dung đến viền -->
</shape>
